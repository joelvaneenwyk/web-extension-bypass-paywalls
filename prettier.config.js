/**
 * Prettier config for Bypass Paywalls browser extension.
 */

/** @type {import("prettier").Config} */
const config = {
  trailingComma: "none",
  tabWidth: 2,
  semi: true,
  singleQuote: true,
  printWidth: 120
};

export default config;
